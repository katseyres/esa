PROGRAMME verificationEmail

email, format, chars : CHAINE

DEBUT
    LIRE(email)
    format <- "xxx@xxx.xx"
    chars <- "abc(...)xyzABC(...)XYZ012(...)789.-"

    SI email[3] != "@" OU email[7] != "." ALORS
        RETOURNE Faux
    FIN

    POUR lettre DANS email FAIRE
        SI chars NE CONTIENT PAS lettre ET (index(lettre) != 3 OU index(lettre) != 7)  ALORS
            RETOURNE Faux
        FIN
    FIN

    RETOURNE Vrai
FIN
